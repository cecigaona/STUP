version: "3.9"

services:
  react-dev:
    build:
      context: .
    working_dir: /app
    command: npm run dev
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    volumes:
      - .:/app
      - node_modules:/app/node_modules

  app:
    build:
      context: ./stup
      dockerfile: Dockerfile.elixir
    container_name: elixir-app-dev
    working_dir: /app
    command: bash -lc "mix deps.get && mix ecto.create && mix phx.server"
    environment:
      MIX_ENV: dev
      PHX_SERVER: "true"
      PHX_HOST: "localhost"
      PORT: "4000"
      # match db service creds + db name
      DATABASE_URL: "ecto://postgres:postgres@db:5432/stup_dev"
      SECRET_KEY_BASE: "dev-secret-key-base-change-me"
      FILE_SYSTEM_BACKEND: "fs_poll"
    ports:
      - "4000:4000"
    volumes:
      - ./stup:/app              # mount only the elixir app, not repo root
      - mix-deps:/app/deps
      - mix-build:/app/_build
    depends_on:
      db:
        condition: service_healthy

  db:
    # Pin 16 for fewer surprises in dev. If you prefer 18+, keep the volume at /var/lib/postgresql (as below).
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: stup_dev
    ports:
      - "5432:5432"
    volumes:
      # for PG16 the classic path works (if you move to 18+, use /var/lib/postgresql)
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d stup_dev || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 15
      start_period: 5s

volumes:
  node_modules:
  mix-deps:
  mix-build:
  db_data:
